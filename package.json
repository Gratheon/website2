{
  "name": "gratheon-website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "../blog-engine-md/blog-engine serve",
    "serve": "../blog-engine-md/blog-engine serve",
    "build": "rm -rf dist && ../blog-engine-md/blog-engine build",
    "publish": "rm -rf build dist && ../blog-engine-md/blog-engine build && mv dist build"
  }
}
